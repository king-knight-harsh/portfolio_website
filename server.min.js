require("dotenv").config();const express=require("express"),cors=require("cors"),path=require("path"),contactRoute=require("./route/contactRoute"),app=express();app.use(express.json()),app.use(cors()),app.use("/",contactRoute),"production"===process.env.NODE_ENV&&(app.use(express.static("client/build")),app.get("*",(req,res)=>res.sendFile(path.resolve(__dirname,"client","build","index.html"))));const port=process.env.PORT||5e3;app.listen(port,console.log("server listing to port 5000 only"));